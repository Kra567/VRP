## About project

App (app.py) demonstrates work of algorithms for solving VRP(vehicle routing problem) in realistic setting (real map of Minsk).

## How to run 

### 1. installing requirements 
`pip install -r requirements.txt`

### 2. running 
`streamlit run app.py`
